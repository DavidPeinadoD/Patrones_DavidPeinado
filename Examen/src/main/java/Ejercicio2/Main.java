package Ejercicio2;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Main {/*
    public static void main(String[] args) {
        // Crear componentes
        Componente cpu = new UnidadCentral("Intel", "Core i5", 250.00, "LGA1200", Arrays.asList(0, 1, 2));
        Componente pantalla = new Pantalla("Samsung", "27\" 4K", 400.00, "HDMI", Arrays.asList(0, 1, 2));
        Componente teclado = new Teclado("Logitech", "K380", 40.00, "Bluetooth", Arrays.asList(0));
        Componente raton = new Raton("Logitech", "M185", 20.00, "USB", Arrays.asList(0));
        Componente tableta = new TabletaGrafica("Wacom", "Intuos Pro", 300.00, "USB", Arrays.asList(0, 1));
        Componente impresora1 = new ImpresoraInyeccion("HP", "OfficeJet 3833", 90.00, "USB", Arrays.asList(0, 1), "HP 302");
        Componente impresora2 = new ImpresoraLaser("Brother", "HL-L2350DW", 120.00, "Ethernet", Arrays.asList(0, 1), "Brother TN-2420");

        // Crear ordenador
        Ordenador ordenador = new Ordenador(cpu, pantalla, teclado, raton);

        // Añadir dispositivos adicionales
        ordenador.addDispositivo(tableta);
        ordenador.addDispositivo(impresora1);
        ordenador.addDispositivo(impresora2);

        // Imprimir información del ordenador
        System.out.println(ordenador.toString());

        // Cambiar componente y puerto de una impresora
        ((ImpresoraLaser) impresora2).setCartucho("Brother TN-2410");
        impresora2.setPuertosValidos(Arrays.asList(0, 1, 2, 3));

        // Imprimir información de la impresora modificada
        System.out.println(impresora2.toString());

        // Imprimir información de la pantalla táctil
        Componente pantallaTactil = new PantallaTactil("LG", "24\" Touchscreen", 350.00, "USB", Arrays.asList(0, 1));
        System.out.println(pantallaTactil.toString());
    }*/
}
